#!/usr/bin/env node

const program = require('commander');

program
  .version(require('../package.json').version)
  .usage('<command> [options]')
  .command('init', 'init template files')
  .command('run', 'run to generate protocols')
  .parse(process.argv);
